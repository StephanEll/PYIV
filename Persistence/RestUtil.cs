using RestSharp;
using System.Collections;
using PYIV.Helper;

namespace PYIV.Persistence{
	

	public class RestUtil
	{
		
		/*
		private static volatile RestUtil instance;
	
	    private static object syncRoot = new Object();
		
		private RestClient client;
	
	    private RestUtil()
			
	    {
			string baseUri = ConfigReader.Instance.GetSetting("server", "url");
	        client = new RestClient(baseUri);
	    }
	
	    public static RestUtil Instance
	    {
	        get
	        {
	            if (instance == null)
	            {
	                lock (syncRoot)
	                {
	                    if (instance == null)
	                    {
	                        instance = new RestUtil();
	                    }
	                }
	            }
	
	            return instance;
	        }
	    }
		
		public RestRequest CreateRequest(string resource, Method method){
			RestRequest request = new RestRequest(resource, method);
			request.RequestFormat = DataFormat.Json;
			
			return request;
		}
		
		*/
		
		
	}

}